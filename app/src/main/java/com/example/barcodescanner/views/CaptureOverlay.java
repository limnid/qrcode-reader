package com.example.barcodescanner.views;

import android.content.Context;
import android.util.AttributeSet;

public class CaptureOverlay extends GraphicOverlay {
    public CaptureOverlay(Context context, AttributeSet attrs) {
        super(context, attrs);
    }
}
